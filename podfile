use_frameworks!

platform :ios, '10.0'

target 'machinetest' do
	pod 'Alamofire', '~> 5.2'
	pod 'Kingfisher'
        pod 'Realm'
        pod 'RealmSwift'
end
#post_install do |installer|
#  installer.pods_project.targets.each do |target|
#    target.build_configurations.each do |config|
#      config.build_settings['SWIFT_VERSION'] = '3.0'
#    end
#  end
#end
